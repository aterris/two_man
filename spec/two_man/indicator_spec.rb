require 'spec_helper'

describe TwoMan::Indicator do

  before do
    #PiPiper ||= double.as_null_object
    #@indicator = TwoMan::Indicator.new()
  end

  it 'defines initializes with off state and 3 LEDs' do
    #@indicator.pins.length.should == 3
    #@indicator.map {|pin| pin.should}
    #PiPiper::Pin.should_receive(:new).with(:pin => 18, :direction => :out)
    #PiPiper::Pin.should_receive(:new).with(:pin => 23, :direction => :out)
    #PiPiper::Pin.should_receive(:new).with(:pin => 25, :direction => :out)
  end

  it 'can turn all LEDs on'

  it 'can turn all LEDs off'

end